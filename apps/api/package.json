{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "lint": "cargo clippy -- -W warnings"
  }
}
